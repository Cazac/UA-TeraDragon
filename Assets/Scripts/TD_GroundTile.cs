using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TD_GroundTile : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Test Code: NEW TILE");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
