using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // GameManager Jobs:
    // Give each tower the proper stats: Base Stats + upgrades ( tower manager)
    // checks tech trees
    // Send each waves (wave/enemy manager)
    // allocate resources to the player
    // remove gems spent and place the tower( tower manager)
    // increase the size of the map every level. (Level manager?)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
